import { BlogHeader, BlogPost } from '@/app/components';

const BlogGrid = () => {
  return (
    <section className="">
      <BlogHeader />
      <BlogPost />
    </section>
  );
};

export default BlogGrid;
